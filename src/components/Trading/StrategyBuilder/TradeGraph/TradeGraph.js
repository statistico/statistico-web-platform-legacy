import React from 'react';

import TradeGraphWrapper from './TradeGraphWrapper';

const TradeGraph = () => {
  return (
    <TradeGraphWrapper>
      <div>Graph data will go in here</div>
    </TradeGraphWrapper>
  );
};

export default TradeGraph;
