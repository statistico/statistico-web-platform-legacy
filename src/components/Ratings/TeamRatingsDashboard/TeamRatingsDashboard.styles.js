import styled from 'styled-components';

const TeamRatingsDashboardWrapper = styled.div`
  display: flex;
  flex-direction: ${(props) => props.direction};
  width: 100%;
`;

export default TeamRatingsDashboardWrapper;
